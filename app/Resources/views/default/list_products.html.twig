{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
    {% include 'default/header.html.twig' %}

    <div class="container mt-5">
        <div class="row mt-5">
            <div class="col-12 mt-5">
                <div class="section_heading text-center mt-5">
                    <h2>Our Products</h2>
                </div>
            </div>
        </div>
    </div>

    {% if(app.request.get('category')=='') %}
    <div class="karl-projects-menu mb-100">
        <div class="text-center portfolio-menu">
            <button class="btn active" data-filter="*">ALL</button>
            <button class="btn" data-filter=".TShirt">T-SHIRTS</button>
            <button class="btn" data-filter=".Jeans">JEANS</button>
            <button class="btn" data-filter=".Chemises">CHEMISES</button>
            <button class="btn" data-filter=".Polo">POLO</button>
            <button class="btn" data-filter=".Mocassin">MOCASSIN</button>
            <button class="btn" data-filter=".Casquettes">CASQUETTES</button>
        </div>
    </div>
    {% endif %}

    {% include 'default/products.html.twig' %}

    {% include 'default/footer.html.twig' %}
    </div>
{% endblock %}