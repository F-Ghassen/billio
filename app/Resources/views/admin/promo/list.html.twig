{% extends 'admin/base.html.twig' %}

{% block title %}{{ parent() }} - List Promo Codes {% endblock %}

{% block body %}

    {{ parent() }}

    <!-- Main content -->
    <div class="content-wrapper">
        <!-- Page header -->
        <div class="page-header">
            <div class="page-header-content">
                <div class="page-title">
                    <h4><i class="icon-arrow-left52 position-left"></i> <span class="text-semibold">Promo codes</span> - List</h4>
                </div>
            </div>

            <div class="breadcrumb-line">
                <ul class="breadcrumb">
                    <li><a href="{{ path('index_admin_page') }}"><i class="icon-home2 position-left"></i> Home</a></li>
                    <li class="active">Promo Codes</li>
                </ul>
            </div>
        </div>
        <!-- /page header -->

        <!-- Content area -->
        <div class="content">

            <!-- Table -->
            <div class="panel panel-flat">
                <div class="row" style="margin: 20px">
                    <a href="{{ path('add_code_page') }}" class="btn btn-success"><i class="icon-plus3 position-left"></i>Ajouter Code Promo</a>
                </div>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>Name</th>
                            <th>Link</th>
                            <th>Montant</th>
                            <th>Code</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for p in codes %}
                            <tr>
                                <td>{{ "%04d"|format(p.id) }}</td>
                                <td>{{ p.name }}</td>
                                <td>{{ p.link }}</td>
                                <td>{{ p.montant }}</td>
                                <td>{{ p.code }}</td>
                                <td>
                                    {% if p.enabled %}
                                        <span class="label bg-success-400">Active</span>
                                    {% else %}
                                        <span class="label bg-danger-400">Disabled</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <ul class="icons-list mt-15">
                                        <li><a href="{{ path('enable_code_page', {'id': p.id}) }}" data-popup="tooltip" title="Visible"><i class="icon-checkbox-checked"></i></a></li>
                                        <li><a href="{{ path('edit_code_page', {'id': p.id}) }}" data-popup="tooltip" title="Edit Promo Code"><i class="icon-pencil5"></i></a></li>
                                        <li><a href="{{ path('delete_code_page', {'id': p.id}) }}" data-popup="tooltip" title="Delete"><i class="icon-trash" onclick="return confirm('Are you sure you want to Delete this Code?')"></i></a></li>
                                    </ul>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>

            </div>
            <!-- /table -->

            {{ include('admin/footer.html.twig') }}

        </div>
        <!-- /content area -->
    </div>
    <!-- /main content -->

    </div>
    <!-- /page content -->

    </div>
    <!-- /page container -->

{% endblock %}